# SQL_Training

## 1. Обзор SQL

### 1.1. Основы СУБД
### 1.2. Краткий обзор SQL

## 2. Выборка данных

### 2.1. Основы SQL
### 2.2. Простые запросы
### 2.3. Соединения таблиц
### 2.4. Множественное соединение таблиц
### 2.5. Итоговые запросы
### 2.6. Подзапросы и выражения

## 3. Обновление данных

### 3.1. Внесение изменений в базу данных
### 3.2. Целостность данных
### 3.3. Транзакции

## 4. Структура базы данных

### 4.1. Создание базы данных
### 4.2. Представление (view)
### 4.3. SQL и безопасность
### 4.4. Системные каталоги

## 5. Программирование и SQL

### 5.1. Встроенный SQL
### 5.2. Динамический SQL

## 6. SQL

### 6.1. Хранимые процедуры
### 6.2. SQL и хранилище данных
### 6.3. SQL и серверы приложений
### 6.4. Сети и распределённые базы данных
### 6.5. SQL и объекты
### 6.6. SQL и XML
### 6.7. Специализированные базы данных

---

### Основные понятия:
- **База данных** — это структурированный набор данных, представленный в виде таблиц.
- **Таблица** состоит из строк (кортежей) и столбцов (атрибутов), отображающих данные об объектах реального мира.

- **Система управления базами данных(СУБД)** -  это комплекс программно-языковых средств, позволяющих создать базы данных и управлять данными. 
Иными словами, СУБД — это набор программ, позволяющий организовывать, контролировать и администрировать базы данных.

- **Реляционная система управления базами данных(РСУБД)** - СУБД управляющая реляционными базами данных.

- **Объектно-реляционная СУБД(ОРСУБД)** — реляционная СУБД, поддерживающая некоторые технологии, присущие объектно-ориентированным СУБД и реализующие объектно-ориентированный подход: объекты, классы и наследование реализованы в структуре баз данных и языке запросов.
## Словарь

### A
- ACID
- ADD COLUMN - добавление нового столбца в таблицу
	**Синтаксис:**
	```
		alter table <имя_таблицы>
		add column <имя_столбца> <тип_данных>
	```
- ALTER TABLE - команда для изменения структуры таблицы(добавление столбцов, индексов, ключей, ограничений)
- AND - логическая операция "**И**" или **конъюнкция**, означает, что все условия должны возвращать **true**, в противном случае результат будет **false**, если хотя бы одно из условий будет **false**;
- AS 
### B
- BETWEEN - оператор для определения интервала значений условия
	**Синтаксис:**
	```
		select <столбец1>, <столбец2>, <столбец3>, ... <столбецN> from <имя_таблицы> where <столбец1> between <начало_интервала> and <конец_интервала>;
	```
- BLOB 
### C
- CHAR(CHARACTER) 
- COALESCE
- CONCAT
- CONCAT_WS
- COUNT
- CREATE DATABASE - команда создания базы данных;
 
	**Синтаксис:**
    ```
		create database <имя_базы_данных>;
    ```
- CREATE TABLE - команда для создания таблицы в бд;

	**Синтаксис:**
    ```
		create table <имя_таблицы> (
			<имя_столбца1> <тип_данных1>,
			<имя_столбца2> <тип_данных2>,
			<имя_столбца3> <тип_данных3>,
			...
			<имя_столбцаN> <тип_данныхN>
		);
    ```
### D
- DATE
- DATETIME
- DECIMAL
- DEFAULT
- DELETE - команда для удаления данных из таблиц бд
	**Синтаксис:**
	```
		delete from <имя_таблицы>
		where <условие>
	```
- DESC
- DESCRIBE 
- DROP TABLE - команда для удаление таблицы из бд;
	**Синтаксис:**
	```
		drop table <имя_таблицы>;
	```
### E
- EXISTS
### F
- FROM
### G
- GROUP BY
### H
- HAVING
### I
- IN - оператор для фильтрации данных, перчисленных в скобках
	**Синтаксис:**
	```
		select <столбец1>, <столбец2>, <столбец3>, ... <столбецN> from <имя_таблицы> where <столбец1> in (<значение1>, <значение2>, <значение3>, ... <значениеN>);
	```
- INSERT INTO - команда для добавления данных в таблицу;
	**Синтаксис:**
	```
		insert into <имя_таблицы>(<имя_столбца1>, <имя_столбца2>, <имя_столбца3>, ... <имя_столбцаN>) 
				values (<значение1>, <значение2>, <значение3>, ... <значениеN>);
	```
- INTEGER(INT) 
- IS NULL - оператор предназначен для определения **null**-значений, он возвращает **true** - если есть **null** значение и **false** - если нет **null** значения;
### J 
- JOIN
### K
### L
- LENGTH - функция возвращает кол-во символов в строке
	**Синтаксис:**
	```
		length(<строка>)
	```
- LIKE - оператор для поиска строк по определенному шаблону с использованием спец символов, так как **%** (любое кол-во разных символов), **_**(один разный символ);
	**Синтаксис:**
	```
		select <столбец1>, <столбец2>, <столбец3>, ... <столбецN> from <имя_таблицы> where <столбец1> like (<%шаблон_для_поиска>);
	```
- LIMIT
- LOWER - функция преобразования всех символов строки к нижнему регистру
	**Синтаксис:**
	```
		lower(<строка>)
	```
- LTRIM - функция возвращает строку с удаленными пробелами в начале строки
	**Синтаксис:**
	```
		ltrim(<строка>)
	```
### M
- MAX
- MIN
### N
- NOT - логическая операция "НЕ" или инверсия, результатом которой получается противоположное, если условие возвращает **true**, то с помощью **not** - получается **false** и наоборот;
- NOT NULL
- NULL
### O
- OR - логическая операция "**ИЛИ**" или дизъюнкция, означает, что если хотя бы одно из условий возвращает **true**, то все выражение возвращает **true**;
- ORDER BY
### P
### Q
### R
- RENAME - команда для переименования таблиц/стоблцов
	**Синтаксис:**
	```
		alter table <старое_имя_таблицы> rename to <новое_имя_таблицы>
	```
- REVERSE - функция переставления символов строки в обратном порядке
	**Синтаксис:**
	```
		reverse(<строка>)
	```
- RTRIM - функция возвращает строку с удаленными пробелами в конце строки
	**Синтаксис:**
	```
		rtrim(<строка>)
	```
### S
- SELECT - команда для выборки данных из таблицы
	**Синтаксис:**
	```
		select <столбец1>, <столбец2>, <столбец3>, ... <столбецN> from <имя_таблицы>;
	```
- SHOW CREATE TABLE
	**Синтаксис:**
	```
		show create table <имя_таблицы>
	```
- SUBSTRING - функция возвращает часть строки, начиная с указанной позиции и длины
	**Синтаксис:**
	```
		substring(<строка>, <начальная_позиция>, <длина_подстроки>)
	```
- SUBSTRING_INDEX - функция возвращает часть строки, до заданного разделителя
	**Синтаксис:**
	```
		substring_index(<строка>, <разделитель>, <номер_подстроки>)
	```
- SUM 
### T
- TIMESTAMP 
### U
- UPDATE - команда для изменения данных в таблицах бд
	**Синтаксис:**
	```
		update <имя_таблицы>
		set <имя_столбца1> = <значение1>
		where <условие1>
	```
- UPPER - функция преобразования всех символов в строке к верхнему регистру
	**Синтаксис:**
	```
		upper(<строка>)
	```
- USE - команда для выбора бд, с которой будут проводится манипуляции
### V
- VARCHAR
- NVARCHAR
### W
- WHEN
- WHERE - команда служить для фильтрации данных в запросе select
	**Синтаксис:**
	```
		select <столбец1>, <столбец2>, <столбец3>, ... <столбецN> from <имя_таблицы> where <столбец1> = <условие_фильтрации>;
	```
- WITH
### X
### Y
### Z
### А
- Атомарность - означает, что таблица бд должна описывать один объект, состоящий из конкретных атрибутов
### Б
### В
### Г
### Д
### Е
### Ё
### Ж
### З
### И 
### К
### Л
### М
### Н
- Нормализация - приведение таблицы к **нормальной форме**(1, 2, 3)
### О
### П
- Первая нормальная форма(**1НФ**) - все атрибуты таблицы простые, имеют скалярные значения и не имеют дубликатов, есть уникальный(**первичный ключ** или **primary key**) идентификатор записей
- Первичный ключ(**Primary key**) - уникальный идентификатор записи, он не может быть пустым или содержать **null**-значения. 
Генерация первичного ключа должна быть автоматической. Ключ должен быть компактным и неизменным(операция **UPDATE** не допустима). 
Существует 2 типа первичных ключей: синтетический(искусственно созданный столбец) и естественный - данные, что уже есть в таблице
### Р
### С
### Т
### У
### Ф
### Х
### Ц
### Ч
### Ш
### Щ
### Э
- Экранирование - данная операция, предназначена для вставки зарезервированных символов в текстовые поля
### Ю
### Я

